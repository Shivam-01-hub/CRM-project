# Partnership CRM — Frontend Environment Variables (Example)
# ============================================================
# Copy this file to .env and adjust values as needed.

# API base URL. In development with Vite proxy, use "/api".
# In production, set to your backend URL.
VITE_API_URL="/api"
